import { Box, styled } from '@mui/material';

export const ScoreControllerWrapper = styled(Box)(() => ({
  display: 'flex',
  flexWrap: 'wrap',
  justifyContent: 'space-between',
  margin: '10px 0',
}));
